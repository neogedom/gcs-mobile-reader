/**
 * Index para o domínio
 * Responsabilidade: Exportar todos os componentes do domínio
 */

// Guards (funcionam bem)
export * from './guards';

// Re-export specific items if needed
export type { Character } from './models/Character';
